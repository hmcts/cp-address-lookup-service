services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    environment:
      - OS_PLACES_API_KEY=${OS_PLACES_API_KEY:?Set OS_PLACES_API_KEY (env var or .env file) before running}
      - OS_PLACES_BASE_URL=${OS_PLACES_BASE_URL:-https://api.os.uk}
